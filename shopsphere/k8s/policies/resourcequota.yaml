apiVersion: v1
kind: ResourceQuota
metadata:
  name: shopsphere-quota
  namespace: shopsphere
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "8"
    limits.memory: 8Gi
    pods: "30"
    services: "10"
    persistentvolumeclaims: "5"
