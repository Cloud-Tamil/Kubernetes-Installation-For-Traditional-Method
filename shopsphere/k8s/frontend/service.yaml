apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: shopsphere
  labels:
    app: shopsphere-frontend
spec:
  selector:
    app: shopsphere-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
